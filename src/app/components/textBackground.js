import React from 'react'

const SectionWrapper = ({ children }) => {
  return <section className="bg-gray-600 bg-opacity-50 p-6 mt-8 rounded-lg shadow-md">{children}</section>
}

export default SectionWrapper
